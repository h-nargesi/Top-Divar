# AGENTS.md

راهنمای کار برای ایجنت‌ها (AI agents) در این ریپازیتوری.

## معرفی پروژه

**Top Divar** — ابزار پایش سایت دیوار (divar.ir) برای کشف آگهی‌های «خوب» (بر اساس فیلترها و معیارهای تعریف‌شده کاربر) و اطلاع‌رسانی فوری از طریق تلگرام.

## وضعیت فعلی

- پروژه از فاز طراحی به **پیاده‌سازی مرحله‌ای** می‌رود. ترتیب کار: [`docs/implementation.md`](docs/implementation.md). هر گفتگو فقط **یک مرحله** را می‌سازد.
- پشتهٔ فناوری مصوب: **Python** (ADR-0011)؛ اجرا: **systemd روی VPS داخل ایران** (ADR-0003، مصوب ۲۰۲۶-۰۹-۱۷، اصلاح ۲۰۲۶-۰۹-۱۸) — تلگرام از طریق `HTTPS_PROXY` روی همهٔ فراخوانی‌های Bot API (ADR-0002)؛ مدل اجرا: asyncio تک‌حلقه با تسک‌های مستقل (ADR-0012).
- مستندات در پوشه `docs/` نگهداری می‌شوند و مرجع اصلی تصمیم‌ها هستند.
- قبل از هر مرحله، [`docs/implementation.md`](docs/implementation.md) و اسناد همان مرحله را بخوانید (از جمله [`docs/deal-scoring.md`](docs/deal-scoring.md) برای معیار کمیابی — خودش در نسخهٔ اول کد نمی‌شود).

## قواعد کلی

- **این repo برای همیشه خصوصی می‌ماند** — دادهٔ شخصی در `config.yaml`/`docs/fetch-sample.md` مجاز است (نمونه‌های خام با نشان `REDACTED` اسکراب می‌شوند)؛ repo عمومی ساخته نمی‌شود (تصمیم ۲۰۲۶-۰۹-۱۸).
- مستندات و توضیحات به **زبان فارسی** نوشته می‌شوند؛ نام‌گذاری کد (متغیر، تابع، فایل) به **انگلیسی**.
- زبان مصوب Python است (ADR-0011). اسکلت را فقط در محدودهٔ مرحلهٔ جاری بسازید؛ مرحلهٔ بعد را پیش‌خور نکنید (`docs/implementation.md`).
- توکن ربات تلگرام و هر کلید/رمز دیگری فقط از طریق `.env` (که در `.gitignore` است) خوانده می‌شود. **هرگز** secret را در کد یا مستندات commit نکنید.
- در تعامل با سایت دیوار به نرخ درخواست معقول توجه کنید (rate limit) و از ارسال درخواست‌های موازی سنگین پرهیز کنید. فاصلهٔ حداقلی شامل هم search و هم جزئیات آگهی (`posts-v2/web`) است.

## دستورات

هنوز دستور build/lint/test وجود ندارد. پس از تعیین پشته فناوری، دستورات مربوطه را اینجا ثبت کنید:

```
# TODO: مثلاً
# pip install -r requirements.txt
# python -m top_divar
# pytest
```

## ساختار پوشه‌ها (پیشنهادی اولیه)

```
docs/        # مستندات + مراحل پیاده‌سازی
src/         # کد منبع (از مرحلهٔ ۱)
tests/       # تست‌ها (همراه هر مرحله)
```
